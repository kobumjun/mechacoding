# 히어로 슬라이드 이미지

메인 페이지 캐러셀에서 사용합니다. **권장 비율 16∶9** (예: 1920×1080). 파일이 없거나 로드에 실패하면 슬라이드 영역은 **배경색 `#1A1A1B`** 로만 보입니다.

| 파일명 | 용도 |
|--------|------|
| `hero-main.jpg` | **슬라이드 1번**에 사용 (기존 메인 히어로 에셋). 동일 목적으로 `hero-slide-01.jpg` 를 두고 코드에서 경로만 바꿔 쓸 수 있음. |
| `hero-slide-02.jpg` | 슬라이드 2번 배경 |
| `hero-slide-03.jpg` | 슬라이드 3번 배경 |

기타 참고용 에셋(캐러셀에 직접 연결하지 않을 수 있음):

- `hero-accent.jpg` — 보조/갤러리용 등
